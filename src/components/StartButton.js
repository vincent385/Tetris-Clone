import React from 'react';
import { StyledStartButton } from './styles/StyledStartBtn'

function StartButton ({ callback }) {
    return (
        <StyledStartButton>Start Game</StyledStartButton>
    );
}

export default StartButton;
